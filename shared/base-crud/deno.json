{
  "name": "@shared/base-crud",
  "version": "0.0.1",
  "private": true,
  "exports": "./mod.ts",
  "tasks": {
    "test": "NODE_ENV=test deno test --allow-all tests/",
    "test:watch": "NODE_ENV=test deno test --allow-all --watch tests/",
    "test:single": "NODE_ENV=test deno test --allow-all tests/",
    "test:coverage": "NODE_ENV=test deno test --coverage=.coverage-tmp --allow-all tests/ && deno coverage .coverage-tmp --include=\".*src/.*\" --exclude=\".*tests/.*\" --html coverage/",
    "test:coverage:lcov": "NODE_ENV=test deno test --coverage=.coverage-tmp --allow-all tests/ && deno coverage .coverage-tmp --include=\".*src/.*\" --exclude=\".*tests/.*\" --lcov coverage/lcov.info",
    "test:coverage:clean": "rm -rf .coverage-tmp",
    "test:seed": "NODE_ENV=test deno run --allow-all tests/seed.ts",
    "test:seed:reset": "NODE_ENV=test deno run --allow-all tests/seed.ts --reset"
  },
  "imports": {
    "elysia": "npm:elysia@^1.4.9",
    "drizzle-orm": "npm:drizzle-orm@^0.44.5",
    "@shared/test-vars": "../test-vars/mod.ts",
    "postgres": "npm:postgres@^3.4.4",
    "@std/assert": "jsr:@std/assert@1"
  }
}
