/**
 * @deprecated Import from resolve-export-zip-password.ts instead.
 * Re-exports kept so existing call sites keep working.
 */
export {
  resolveUserDownloadZipPassword,
  resolveExportZipPassword,
  ZIP_PIN_REQUIRED,
  ZIP_DOSSIER_PASSWORD_REQUIRED,
  type ZipPasswordSource,
  type ResolvedExportZipPassword,
} from "./resolve-export-zip-password.ts";
