/** MinIO prefix segment for draft scans (no project until promote). */
export const SCAN_DRAFT_WORKSPACE = '_workspace'

/** Max pages per ADF/duplex batch (agent uploads to pre-allocated presigned URLs). */
export const SCAN_BATCH_MAX_PAGES = 40

export const SCAN_SESSION_STORAGE_KEY = 'sohoa-scan-intake-session-id'

export const DEFAULT_DOC_SLUG = 'Tai_lieu_1'
export const DEFAULT_DOC_NAME = 'Tài liệu 1'
